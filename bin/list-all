#!/usr/bin/env bash

set -e

# shellcheck source=../bin/utils.sh
source "$(dirname "$0")/utils.sh"

list_all() {
    ensure_kerl_setup

    "$(kerl_path)" list releases | sed -e 's:Run.*::' | tr '\n' ' '
}

list_all
